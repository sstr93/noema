import {getChatGPTUser,chatGPTSignInPath} from '@/app/chatgpt-auth';import {isAdmin} from '@/lib/security';import {getArticles,database} from '@/lib/db';import {mailReady,Contact} from '@/lib/mail';import {Header,Footer,Logo} from '@/components/magazine';import Editor from './editor';
export const dynamic='force-dynamic';export const metadata={title:'Área editorial',robots:{index:false,follow:false}};
export default async function Admin(){const user=await getChatGPTUser();if(!user)return <><Header/><main className="admin-signin"><Logo color="blue"/><span className="eyebrow">ACESSO À REDAÇÃO</span><h1>O próximo texto começa aqui.</h1><p>Entre com a conta autorizada para criar, editar e publicar artigos.</p><a href={chatGPTSignInPath('/admin')} target="_top">Entrar com ChatGPT ↗</a></main><Footer/></>;if(!await isAdmin())return <><Header/><main className="admin-signin"><h1>Acesso restrito à redação.</h1><p>Esta conta não tem permissão para editar a revista.</p><a href="/signout-with-chatgpt?return_to=/admin" target="_top">Trocar de conta</a></main><Footer/></>;try{const [articles,contacts]=await Promise.all([getArticles(true),database().prepare('SELECT * FROM contacts ORDER BY created_at DESC LIMIT 100').all<Contact>()]);return <><Header/><Editor initial={articles} contacts={contacts.results} mailReady={mailReady()}/><Footer/></>}catch{return <><Header/><main className="empty-page"><h1>A redação está temporariamente indisponível.</h1><p>Não foi possível carregar os textos. Tente novamente em instantes.</p></main><Footer/></>}}
